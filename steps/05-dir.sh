#!/bin/sh

log "Creating directories..."

mkdir -p \
    "$HOME/.local/bin" \
    "$HOME/.config" \
    "$HOME/.cache" \
    "$HOME/dl" \
    "$HOME/dox" \
    "$HOME/music" \
    "$HOME/pix" \
    "$HOME/vid" \
    "$HOME/code" \
    "$HOME/projects" \
    "$HOME/personal"

log "Directories created"
